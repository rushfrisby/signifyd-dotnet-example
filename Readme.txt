Replace the APIKEY in the app.config with the key found in your settings page. 

Program.cs, has the example get request by caseId and a Post request to create the investigation. 

To create the post body you will need to map the fields in the model objects to send us the order information as part of this post request.